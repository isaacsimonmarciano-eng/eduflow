import { getAuthUserId } from "@convex-dev/auth/server";
import { v } from "convex/values";
import { mutation, query } from "./_generated/server";

export const list=query({args:{homeworkId:v.id("homework")},handler:async(ctx,{homeworkId})=>{const uid=await getAuthUserId(ctx);if(!uid)return[];const me=await ctx.db.get(uid);const hw=await ctx.db.get(homeworkId);if(!hw||!me?.className||hw.className!==me.className)return[];const rows=await ctx.db.query("homeworkNotes").withIndex("by_homework",q=>q.eq("homeworkId",homeworkId)).collect();const out=[];for(const n of rows){const a=await ctx.db.get(n.authorId);out.push({...n,authorName:a?.name??"Un élève",mine:n.authorId===uid});}return out.sort((a,b)=>a.createdAt-b.createdAt);}});
export const add=mutation({args:{homeworkId:v.id("homework"),text:v.string()},handler:async(ctx,{homeworkId,text})=>{const uid=await getAuthUserId(ctx);if(!uid)throw new Error("Connecte-toi d'abord.");const me=await ctx.db.get(uid);const hw=await ctx.db.get(homeworkId);if(!me?.className||!hw||hw.className!==me.className)throw new Error("Devoir introuvable.");const clean=text.trim().slice(0,4000);if(!clean)throw new Error("Écris une note.");return ctx.db.insert("homeworkNotes",{homeworkId,className:me.className,authorId:uid,text:clean,createdAt:Date.now(),updatedAt:Date.now()});}});
export const edit=mutation({args:{id:v.id("homeworkNotes"),text:v.string()},handler:async(ctx,{id,text})=>{const uid=await getAuthUserId(ctx);if(!uid)throw new Error("Connecte-toi d'abord.");const n=await ctx.db.get(id);if(!n||n.authorId!==uid)throw new Error("Tu ne peux modifier que ta propre note.");await ctx.db.patch(id,{text:text.trim().slice(0,4000),updatedAt:Date.now()});}});
export const remove=mutation({args:{id:v.id("homeworkNotes")},handler:async(ctx,{id})=>{const uid=await getAuthUserId(ctx);if(!uid)throw new Error("Connecte-toi d'abord.");const n=await ctx.db.get(id);if(!n)return;if(n.authorId!==uid)throw new Error("Tu ne peux supprimer que ta propre note.");await ctx.db.delete(id);}});
